<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="levelsoftware.io.androidplayground">

  <application
      android:name="io.levelsoftware.androidplayground.PlaygroundApplication"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/PlaygroundTheme">

    <activity
        android:name="io.levelsoftware.androidplayground.activity.PlaygroundActivity"
        android:theme="@style/PlaygroundActivityTheme"
        android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
        >
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

  </application>

</manifest>